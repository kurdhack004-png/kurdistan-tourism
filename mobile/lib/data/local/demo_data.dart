import '../../features/bookings/models/booking.dart';

/// Kept only for the local booking UI shape. Real tourism locations and
/// accommodations must come from the backend or the location cache; the app
/// must never present fabricated businesses, prices, ratings, or coordinates.
final demoBookings = <Booking>[];
