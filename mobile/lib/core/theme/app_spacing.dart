/// Spacing scale (rem-like, 4px base unit) so every screen shares the
/// same rhythm instead of hand-picked paddings.
class AppSpacing {
  AppSpacing._();

  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 14.0;
  static const lg = 20.0;
  static const xl = 32.0;

  static const cardRadius = 14.0;
  static const chipRadius = 8.0;
}
