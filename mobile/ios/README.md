The Flutter source is shared with iOS. iOS native runner generation requires macOS + Xcode. On a Mac, from the project root run:
`flutter create --platforms=ios .`
then `flutter build ipa --release`.
